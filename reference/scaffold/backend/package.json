{
  "name": "personal-cognitive-os-backend",
  "version": "0.1.0",
  "description": "API gateway: auth, RBAC, audit logging, validation. Serves web + mobile clients.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
